name: KV260 Physical Lab Checks

on:
  push:
    paths:
      - "labs/**"
      - "boards/kv260/**"
      - "docs/en/KV260_UDMABUF_SETUP.md"
      - "docs/zh/KV260_UDMABUF_SETUP.md"
      - ".github/workflows/physical-labs.yml"
  pull_request:
    branches:
      - main
    paths:
      - "labs/**"
      - "boards/kv260/**"
      - "docs/en/KV260_UDMABUF_SETUP.md"
      - "docs/zh/KV260_UDMABUF_SETUP.md"
      - ".github/workflows/physical-labs.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  lab-contracts:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          version: "0.12.17"
          enable-cache: true
      - name: Sync Python environment
        run: uv sync --group dev
      - name: Install Tcl and open-source HDL tools
        run: |
          sudo apt-get update
          sudo apt-get install -y tcl iverilog verilator yosys
      - name: Check Physical Lab notebooks, runtime helpers, and simulations
        run: uv run pytest labs/checks -q
      - name: Lint and synthesize KV260 teaching RTL
        run: bash scripts/check_kv260_rtl_static.sh
