name: Python exercise infrastructure

on:
  push:
    paths:
      - "exercises/**"
      - "scripts/start_exercise.py"
      - "lessons/**"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/python-exercises.yml"
  pull_request:
    paths:
      - "exercises/**"
      - "lessons/**"
      - "scripts/start_exercise.py"
      - "pyproject.toml"
      - "uv.lock"
      - ".github/workflows/python-exercises.yml"

jobs:
  exercise-checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true
      - name: Sync environment
        run: uv sync --group dev
      - name: Check graders and notebook structure
        run: uv run pytest exercises/checks/test_graders.py exercises/checks/test_notebook_structure.py exercises/checks/test_start_exercise.py exercises/checks/test_rtl_bridge_student_flow.py exercises/checks/test_platform5_student_flow.py lessons/checks/test_platform4_notebook_structure.py lessons/checks/test_platform4_student_dryrun.py lessons/checks/test_platform5_notebook_structure.py lessons/checks/test_platform5_student_dryrun.py -q
